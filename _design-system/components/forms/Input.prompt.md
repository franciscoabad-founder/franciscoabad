Labeled text field with a glowing Ultramarine focus ring. Works on dark and light surfaces.

```jsx
<Input label="Email" type="email" placeholder="tu@empresa.com" hint="Te respondo en 48h." />
<Input label="Nombre" invalid hint="Campo requerido" />
```

Spreads native `<input>` props (placeholder, value, onChange, required…).

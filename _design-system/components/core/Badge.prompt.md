Small uppercase pill for eyebrows, section labels, categories and status.

```jsx
<Badge tone="accent">Empieza aquí</Badge>
<Badge tone="metric">+18.000</Badge>
```

Tones: `accent` (Ultramarine tint, default) · `solid` · `metric` (champagne, only when the chip carries a number) · `neutral`.

The brand's signature proof number: a large Champagne figure with a caption. Champagne is reserved for these and KPIs · never for general accents.

```jsx
<MetricStat value="32.000" prefix="+" label="empleados bajo liderazgo en el IESS" />
<MetricStat value="10" prefix="$" suffix="B+" size="lg" align="center" label="fondos administrados" />
```

Pre-format `value` yourself (locale separators, "36 → 78"). Sizes `sm` `md` `lg`.
